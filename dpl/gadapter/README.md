# gadapter - convert graph

## saved model
TF and TF-SERVING model

Saving saved_model from `input_model` with version number from `model.yaml`

```bash
saved_model/${version}/varables*
saved_model/${version}/graph.pb
```

## tfgraph
Frozen graph

## tflite
TF-Lite model

## tftrt
TF-TRT model
